/*
    7.15 Utilize um array unidimensional para resolver o seguinte problema.
    Leia 20 números, cada um dos quais está entre 10 e 100, inclusive.
    À medida que cada número é lido, valide-o e armazene-o no array somente se
    ele não for uma duplicata de um número já lido. Depois  de ler todos os valores,
    exiba somente os valores únicos que o usuário inseriu. Previna-se para o ‘pior caso’
    em que todos os 20 números  são diferentes. Utilize o menor array possível para
    resolver esse problema.
    Deitel, Harvey; Paul, Harvey. C++ Como Programar (p. 303). Edição do Kindle.
    Autor: Pedro Filho, 24/10/2021
*/

#include <iostream>
#include <locale>
#include <ctime>

using namespace std;

// função principal
int main()
{
    // localização geográfica
    setlocale( LC_ALL, "portuguese");

    // limpa a tela
    system("cls");

    system("pause"); // pausa do programa

    return 0; // programa terminado com sucesso

} // fim main
